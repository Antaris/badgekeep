import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create a badge profile",
};

export default function NewPackLayout({ children }: { children: React.ReactNode }) {
  return children;
}
