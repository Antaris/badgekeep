# Optional. If set, “Send if Resend is configured” really sends mail.
# RESEND_API_KEY=
# RESEND_FROM=BadgeKeep <noreply@yourdomain.com>

# Optional. Signs carer share tokens. A prototype default is used if unset.
# SHARE_SECRET=
